apiVersion: v1
kind: Service
metadata:
  name: microservices-service
  namespace: microservices
spec:
  type: LoadBalancer
  selector:
    app: microservices-app
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP